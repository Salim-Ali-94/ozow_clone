import { StyleSheet } from "react-native";
import * as constants from "../../utility/constants";


export const styles = StyleSheet.create({

    line: {

        borderBottomColor: constants.cement,
        borderBottomWidth: 0.7
    }

});
